<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.taglibrary.mapper.TlTagDirMapper">

    <resultMap type="TlTagDir" id="TlTagDirResult">
        <id     property="dirId"      column="dir_id"      />
        <result property="libraryId"  column="library_id"  />
        <result property="parentId"   column="parent_id"   />
        <result property="dirName"    column="dir_name"    />
        <result property="orderNum"   column="order_num"   />
        <result property="delFlag"    column="del_flag"    />
        <result property="createBy"   column="create_by"   />
        <result property="createTime" column="create_time" />
        <result property="updateBy"   column="update_by"   />
        <result property="updateTime" column="update_time" />
        <result property="remark"     column="remark"      />
    </resultMap>

    <sql id="selectVo">
        select dir_id, library_id, parent_id, dir_name, order_num, del_flag,
               create_by, create_time, update_by, update_time, remark
        from tl_tag_dir
    </sql>

    <select id="selectDirList" parameterType="Long" resultMap="TlTagDirResult">
        <include refid="selectVo"/> where library_id = #{libraryId} and del_flag = '0'
        order by parent_id, order_num, dir_id
    </select>

    <select id="selectDirById" parameterType="Long" resultMap="TlTagDirResult">
        <include refid="selectVo"/> where dir_id = #{dirId} and del_flag = '0'
    </select>

    <insert id="insertDir" parameterType="TlTagDir" useGeneratedKeys="true" keyProperty="dirId">
        insert into tl_tag_dir (
            library_id, parent_id, dir_name, order_num, create_by, create_time, remark
        ) values (
            #{libraryId}, #{parentId}, #{dirName}, #{orderNum}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateDir" parameterType="TlTagDir">
        update tl_tag_dir
        <set>
            <if test="libraryId != null">library_id = #{libraryId},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="dirName != null">dir_name = #{dirName},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        where dir_id = #{dirId}
    </update>

    <update id="deleteDirById" parameterType="Long">
        update tl_tag_dir set del_flag = '2', update_time = sysdate() where dir_id = #{dirId}
    </update>

    <select id="countTagByDirId" parameterType="Long" resultType="int">
        select count(1) from tl_tag where dir_id = #{dirId} and del_flag = '0'
    </select>

</mapper>

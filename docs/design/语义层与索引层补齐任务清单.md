# 标签语义层与检索索引层 · 补齐任务清单

> 依据：`docs/design/标签语义层与检索索引建设方案.md`（2026-09-17 评审版）+ `docs/design/语义层+索引层建设步骤.md`
> 生成：2026-09-19 现状核对（试点 31/969 已闭环、主干未打通）
> 用法：按批次推进；`[阻断]` 表示后续任务的硬前置
> 红线（任何批次都不得违反）：不改宽 `tl_tag`；不恢复 `dp_code_table` 等五张无主表；不删除旧码值接口；权限不写入快照/索引；"超过100万"不得近似为档位集合；31/969 不得写成 100%；封存测试题不进别名/示例/LLM/反馈

---

## 0. 状态基线（2026-09-19 核对结论）

| 层 | 已有 | 缺 |
|---|---|---|
| 数据库 | `V20260918_01` 11 张 `ts_*` 表 + 菜单 2140–2143；`V20260918_02` 40 条词典种子 + 2144 发布权限 | 运行态未核验；`ts_tag_profile` 无写入方；`ts_retrieval_feedback` 无接口 |
| Java | `TsSemanticController` 约 30 端点；`TsSemanticServiceImpl` / `TsCatalogRuntimeServiceImpl` / `TsBootstrapExportService` / `TsSnapshotCanonicalizer` / `TsCompletenessScorer`；13 个测试类 | 快照 JSONL 不落盘；publish 只导出 tag 骨架；缺 download / stats / feedback 接口 |
| Python | `ai-runtime/tag_semantic/` 约 2700 行：rule_init → concept_cluster → snapshot → local 索引 → 六通道检索 → eval → CLI；pytest 35 passed | Milvus store 为 stub；embedder 是 64 维 hash；无 reranker；无 HTTP 服务；依赖为空 |
| 产物 | 试点 `out/`（31 标签 + 543 码值 + 17 概念，gitignore 未入库）；`docs/design/s3_concept_review_pack.md` | 快照文件、Milvus Collection、alias_automaton.pkl、封存评测集、三类质量报告 |
| 环境 | `docker/milvus`（v3.0.1 栈，本机 19530/9091 在跑）+ `docker/redis` | Milvus 未接入代码；生产资源与监控未评 |

四类交付物（步骤文档 §二）：① 全量语义资产 🟡；② 不可变 snapshot.jsonl 🔴；③ 检索产物 🟡；④ 质量报告 🔴。

### 执行核验记录（2026-09-19）

- 基线测试：`mvn test -pl ruoyi-taglibrary -am -q`，数据源管理 29、对象群 58、标签库 77，共 164 项通过；在 `ai-runtime` 下执行 `./.venv/bin/python -m pytest -q`，35 项通过。仅为已有代码单元测试，不代表整层建设通过验收。
- 运行环境：本机 MySQL 3306；外部已启动的 Redis 6379（PONG）与 Milvus v3.0.1（健康检查 200）。按用户要求复用外部服务，不执行项目 Docker 启停命令。
- 应用启动：`./dev.sh start` 成功，后端 8080、前端实际端口 1024；日志分别为 `logs/ruoyi-admin.jar-dev.log`、`logs/ruoyi-ui-dev.log`。
- T0-4 部分证据：三个指定接口的未认证访问均返回 HTTP 200 / 业务码 401。用户正常登录后，`tag/list` 总数 31，`eligible-tags` 返回 969；`snapshot/active` 明确返回“库内无 ACTIVE 快照”，指定不存在的快照时拒绝服务，不存在的库返回错误。日志已出现 `ts_tag_semantic`、`ts_catalog_snapshot` SQL。非管理员缺权限账户、在线版本不可用场景尚未实测，故不勾选 T0-4。
- 当前 `bootstrap/export` 只读调用成功：970 标签（含对象键）、1723 码值、9 个根目录、issues 为空；数据集 6 / 版本 12。响应字节哈希 `8a961729c53d51f9e6fa86503f8314c114ee74b34b396e374f1d30fd7db5d281`。尚未持久化正式 freeze，也未补齐一致性事务与来源 manifest，不能视为 T2-1 完成。
- B0 尚未通过：概念为空；受控草稿导入被工具权限层拦截，未发生写入。未执行迁移、试点导入或复核，不以仓库 SQL 替代数据库实测。
- 试点口径纠正：接口回读的 31 条 `ts_tag_semantic` 含对象键 `CUST_ID`（tag_id 525），实际为 30 条业务语义草稿；均未复核，不能据此宣称已有 31/969 的生产语义覆盖。
- B1 新发现：现有 Java/Python 规范化均错误排除标签/概念的业务 `status`，且嵌套信封字段处理不一致。需补回归并修正规范化；`content_hash` 为规范化内容哈希，下载头 `X-Content-Sha256` 应为原文件字节校验和，两者不能直接比较。
- B3 新发现：`build_index` 实际始终实例化 `LocalStore`，却直接把调用参数 `store_type` 写入 manifest；实现真实模式选择前必须拒绝不支持的存储声明，不能生成伪 MILVUS 构建记录。

| B0 只读核验项 | 实测 | 差异 / 待确认 |
|---|---|---|
| 迁移与菜单 | `V20260918_01/02` 均已有迁移记录，仓库当前 7 条迁移均已记录；11 张 `ts_*` 表，菜单 2140–2144 齐全且启用 | 无待迁移脚本，不重复执行迁移 |
| 库 107 | 970 个未删除标签，969 个业务标签 + 1 个对象键；均上线且 AVAILABLE | 与业务分母 969 一致 |
| 在线数据集 | 数据集 6、数据源 101；默认版本 12（版本号 1）ONLINE，970 个启用字段 | 包含对象键，不能计为 970 个业务标签 |
| 默认码表 | 关系 2 → 维表 2，排序 1；`L_INDVCST_LABEL_CODE_MAP` 启用、未删除、同源 101 | 已绑定，无须重跑码表覆盖脚本 |
| 物理宽表 | 970 列：DECIMAL 449 / INT 160 / TEXT 155 / TINYINT 129 / DATE 66 / DATETIME 10 / VARCHAR 1 | 清单的 969 是业务列口径；物理表另有对象键列 |
| 标准码表 | 177 个字段名，1723 行 | 与方案假设一致；区间/层级码义仍需业务复核 |
| 语义表（全表聚合） | `ts_concept` 0；`ts_tag_semantic` 31（DRAFT）；`ts_code_value_semantic` 543（DRAFT）；`ts_alias` 0；`ts_business_term` 40（REVIEWED） | 31/543 仅数量吻合，未逐 ID/内容对账；17 个概念候选尚未入库，不能把离线试点标记为已发布 |

---

## 1. 批次总览

| 批次 | 内容 | 阻断性 | 预估 |
|---|---|---|---|
| B0 运行态核验 | 迁移落库、S0 只读核验、冒烟 | `[阻断]` 一切 | 1–2 人日 |
| B1 快照契约 | JSONL 落盘 + download + 快照组装对齐 | `[阻断]` Python 检索 | 3–5 人日 |
| B2 语义全量 | 真实 export、rule_init 全量、概念/码值复核、LLM 富化、画像 | `[阻断]` 覆盖率验收 | 10–15 人日（含业务复核） |
| B3 索引层 | Milvus 实现 + PoC、真实 Embedding/Reranker、产物补全 | `[阻断]` 检索质量 | 8–12 人日 |
| B4 服务化 | FastAPI + Spring Boot 调用链 + Trace 回写 | `[阻断]` 端到端 | 5–7 人日 |
| B5 反馈闭环 | feedback 接口 + ⑥ 通道 + 飞轮流程 | | 3–4 人日 |
| B6 前端 | 语义维护页 + 快照与索引页 + 菜单 | | 6–8 人日 |
| B7 评测验收 | 封存集 600 + 契约回归 + 三类报告 | `[阻断]` 竞赛验收 | 8–10 人日 |
| B8 运维 | 激活协议加固、清理、监控、备份重建 | | 5–6 人日 |

依赖：B0 → B1 → {B2, B3 并行} → B4 → {B5, B6} → B7 → B8。

---

## 2. 任务明细

### B0 运行态核验（`[阻断]`）

- [x] **T0-1 迁移落库**
  - 实测：`schema_migration` 已记录 `V20260918_01/02`；11 张 `ts_*` 表、菜单 2140–2144 全部存在且启用。仓库当前 7 条迁移均已记录，本次不重复执行迁移。
  - 交付：既有迁移记录与在线库结构核验；未重跑 `bin/db-migrate.sh`。
  - 验收：`select version from schema_migration` 含两条；`show tables like 'ts\_%'` = 11；`select menu_id,menu_name,perms from sys_menu where menu_id between 2140 and 2144` = 5 行。
  - 参考：AGENTS.md「DB init & migration」

- [x] **T0-2 S0 在线库只读核验**（方案 §8-S0、§1.1）
  - 现状：`pilot_freeze.jsonl` 的 `source=s0_inventory+repo_sql`，来自仓库 SQL 文本而非在线库
  - 交付：核验记录（实测 vs 方案假设），含：`L_INDVCST_LABEL` 列数与类型分布（969/TINYINT129/TEXT155/DECIMAL449/INT160/DATE66/DATETIME10）、`L_INDVCST_LABEL_CODE_MAP` 是否入库及实际码值数（假设 177 字段/1723 码值）、库 107 数据集/ONLINE 版本/启用字段、`tl_tag_library_dimension`→`dp_dimension_table` 默认码表绑定（大码表是否已绑）
  - 验收：每项给出实测值、与方案差异、待确认清单；**不盲目重跑 02~05 覆盖码值**
  - 参考：方案 §1.1、§1.2、§8-S0

- [ ] **T0-3 试点语义数据是否在库**
  - 交付：`ts_concept / ts_tag_semantic / ts_code_value_semantic / ts_alias / ts_business_term` 的行数与 `review_status` 分布
  - 若为空：走 `ai-runtime/bootstrap/jsonl_to_sql.py` 生成 `rule_init_import.sql` 手工执行，或走 Java `POST /bootstrap/import`（受控路径，优先）
  - 验收：31 标签 + 543 码值 + 17 概念可查

- [ ] **T0-4 后端冒烟**
  - 命令：`GET /taglibrary/semantic/tag/list?libraryId=107`、`/eligible-tags?libraryId=107`、`/snapshot/active?libraryId=107`
  - 验收：权限生效；日志出现 `ts_*` SQL；`eligible-tags` 在无法取得资格时**拒绝服务**而非返回全量

### B1 快照契约打通（`[阻断]`，方案 §6.1 / §6.3 / §11）

- [ ] **T1-1 快照 JSONL 落盘 + `storage_uri` 真实化**（P0）
  - 现状：`ts_catalog_snapshot` **无 jsonl 列**、mapper 不写；`storage_uri="memory://<id>"` 是占位；JSONL 生成后丢弃
  - 方案：新增迁移 `V<日期>_01__tag_snapshot_artifact.sql`（幂等），写文件 `<tag.snapshot-dir>/<snapshotId>.jsonl`（新增配置项），`storage_uri=file://…`，记录文件字节校验和；DB 不存大字段
  - 备选（临时）：`alter table ts_catalog_snapshot add column jsonl longtext`——若采用须在方案附录标注为偏差
  - 验收：一次 publish 后文件存在、`sha256` 与 `content_hash` 双记录；重发同内容复用或新序号

- [ ] **T1-2 publish 完整性补齐**（P0，实为架构不一致）
  - 现状：Java `publish()` 只导出 `kind=meta` + `kind=tag` 骨架（tag_id/field_name/name/semantic_type/family_key/concept_id），**不含 concept / alias / code_value / term / confusable / example**，且 `concept_count/alias_count/code_value_count` 从未赋值；Python `snapshot/publish.py` 才做完整组装
  - 方案 §6.1 明确"Java 导出 → Python 消费，Python 不得回查实时库"。正解：**Java publish 补全组装**（新增各 `ts_*` 表 REVIEWED 查询：概念含祖先、标签嵌入别名/易混淆/正反例/画像摘要、码值行、term 行），Python `assemble_snapshot` 降级为离线/试点工具
  - 验收：Java 快照经 `load_catalog` 后，`catalog.tags/concepts/code_values/terms/aliases` 全非空，且计数与 `ts_*` 表 REVIEWED 行数一致

- [ ] **T1-3 `GET /taglibrary/semantic/snapshot/{snapshotId}/download`**（方案 §11）
  - 交付：流式下载 + `X-Content-Sha256` 响应头 + 权限（`taglibrary:semantic:list` 或 `publish`）
  - 验收：仅授权服务可下载全量快照；下载内容 hash 与 `content_hash` 一致

- [ ] **T1-4 Python 取快照通道**
  - 交付：`snapshot/fetch.py` 或 `cli fetch-snapshot --library 107 --base-url …`：下载 → `load_catalog(expected_hash)` 校验 → 落盘
  - 验收：端到端一次成功；hash 不符时明确报错不静默继续

- [x] **T1-5 快照契约回归**
  - 交付：把现有 hash 向量（`docs/design/snapshot_hash_vector.json` + Java `TsSnapshotCanonicalizerTest` + Python `canonicalize.py`）扩展为**端到端**用例：Java 生成 → Python 校验
  - 验收：CI/本地可重复；发布信封字段（snapshot_id/snapshot_no/generated_at/content_hash）不参与内容哈希

- [x] **T1-6 快照 Schema 校验**
  - 交付：Python `snapshot/schema.py` 补完整性检查（引用闭合：别名指向的 tag/concept/code 必须存在；易混淆对内容一致；码值 `path` 派生）
  - 验收：缺引用/重复对/悬空概念时拒绝加载

### B2 语义资产全量（方案 §8-S2~S6、§9）

- [ ] **T2-1 真实 freeze 导出**
  - 交付：调 `POST /taglibrary/semantic/bootstrap/export`（在线库 + 活动码表关系），产出全量 `freeze.jsonl`；`source_manifest` 记录数据集版本、码表来源、内容哈希、冻结时点
  - 验收：`tagCount/codeValueCount/domainCount` 与 T0-2 核验一致；同源多码表在同一一致性读中批量读取

- [x] **T2-2 rule_init 全量 + unresolved 分类**
  - 交付：全量 `rule_init_result.jsonl` + `unresolved.jsonl`（当前 22 → 全量规模），按 时间/统计/单位/来源/复合口径 分类
  - 验收：未命中项进入待复核清单；"历史最高"= HIST+MAX；"金额"不默认 SUM

- [ ] **T2-3 概念/族全量定稿**
  - 现状：`concept_cluster.py` 的 `PILOT_CONFIRMATIONS` 硬编码 31 条试点确认
  - 交付：改为配置/复核包驱动；产出全量概念候选 + review pack（`docs/design/s3_concept_review_pack.md` 全量版）；人工确认 `concept_code / domain_dir_id / family_key / caliber_variant`
  - 验收：约 300 概念定稿；`family_key = concept_code|statistic|scope|source_system|unit|caliber_variant`，空限定写 NONE；EOP 与 MAX 不混族

- [ ] **T2-4 码值语义全量复核**
  - 交付：区间端点/排序/层级/未知桶全部经业务确认后才 REVIEWED；跨桶阈值用 `interval_covers` 检出并标记"不可精确表达"
  - 验收：有序区间端点/重叠/缺口检查通过；机构层级仅在有权威关系时启用，否则降级 `ENUM_NOMINAL`

- [ ] **T2-5 S5 LLM 富化接真实模型**
  - 现状：`llm_enrich.py` 只有 `stub_generate`（护栏 `validate_enrichment` 已实现）
  - 交付：模型/供应商接入 + 数据边界确认 + 脱敏输入（不给客户原值/封存题）+ 成本评估 + 小批质量验证后再全量
  - 验收：输出全部 `source=LLM, review_status=DRAFT`；含禁写字段即拒绝

- [ ] **T2-6 S4 画像接入**
  - 现状：`ts_tag_profile` 表在、Python `profile/aggregator.py` 在，**Java 无 domain/mapper/接口，无调度**
  - 交付：Java 读写 + 聚合调度（日批或手动触发）+ 小样本抑制 + 敏感等级过滤（TEXT_FREE/ID_KEY/UNKNOWN 不输出）
  - 验收：`ts_tag_profile` 有数据；对象键与自由文本无原值

- [ ] **T2-7 S6 复核工作流**
  - 交付：按概念/族展示草稿 + 来源差异 + 待确认码义的查询接口（前端依赖）；review 接口已存在
  - 验收：修改已复核内容回落 DRAFT；`basis_hash` 漂移检测生效

- [ ] **T2-8 发布门禁补全**（方案 §9.2）
  - 交付：覆盖率分母、完整度阈值、码值冲突、悬空引用、依据漂移 → 结构化 `quality_report`（含排除项与真实计数）
  - 验收：门禁与 JSONL 导出**使用同一批冻结数据**，不各自重读实时库

### B3 索引层（`[阻断]`，方案 §7.2/§7.3/§7.7）

- [ ] **T3-1 存储模式决策与声明**
  - 交付：Milvus PoC 未过前，明确 LOCAL 为唯一模式且**禁止自动降级**（`milvus_store` 已抛异常，需在 `activeBundle`/Trace 标注 `store_type`）
  - 验收：`store_type` 在快照/构建/请求三层可追溯

- [ ] **T3-2 `milvus_store` 实现**（方案 §7.7.3）
  - 交付：Collection schema（doc_id/doc_type/library_id/tag_id/concept_id/family_key/…/name_text/body_text/sparse_name/sparse_body/dense）、两条 BM25 Function、dense FLAT+COSINE、标量索引、每 build 一 Collection、alias 切换、回读全部 `doc_id` 排序哈希对账
  - 验收：行集与快照一致（`count` 相等不算通过）；空集/白名单过滤与 LOCAL 行为一致

- [ ] **T3-3 W1 PoC 测量**（方案 §7.7.4）
  - 清单：镜像/补丁/SDK 锁版本与 arm64、jieba 自定义词典 `run_analyzer`、FLAT 精确基线召回、tag/concept 白名单（含空集、3000 项）过滤 P95、激活故障与补偿、资源峰值
  - 验收：有测量数据与"不通过处置"，不硬凑 969/1723

- [ ] **T3-4 依赖补齐与锁版本**
  - 现状：`ai-runtime/pyproject.toml` `dependencies = []`，venv 只有 pytest
  - 交付：numpy / pymilvus / fastapi / uvicorn / jieba / 模型推理库；锁版本；记录镜像摘要
  - 验收：`pip install -e .` 后可直接跑 `cli` 与测试

- [ ] **T3-5 真实 Embedding + Reranker**
  - 现状：`HashEmbedder` 64 维确定性哈希占位；**无 reranker**
  - 交付：BGE-M3 embedder（替换）、bge-reranker-v2-m3 重排阶段（候选 50 → Top 10，注入族信息与易混淆说明后再打分）、模型 hash 入 `manifest` 与 `ts_index_build`
  - 验收：开发集 A/B 记录；换模型即新 build，快照不退役

- [x] **T3-6 产物补全与持久化**
  - 现状：`alias_automaton.pkl` 不存在（内存 dict + 子串匹配）；docs 向量未持久化
  - 交付：AC 自动机（长词优先、单字需上下文）落盘；docs 向量单独 `emb.npy`；`manifest` 扩展 `analyzer_version / retrieval_config_hash / eval_summary`；如声明降级则预构建 `local_fallback/`
  - 验收：同 build 可离线重建、可校验

- [ ] **T3-7 hybrid_search 评估**（可选，方案 §7.3）
  - 验收：召回/特征差异报告后再决定是否启用，不占首期关键路径

### B4 服务化与调用链（`[阻断]`）

- [x] **T4-1 FastAPI 服务**（方案 §7.6）
  - 交付：`ai-runtime` HTTP 入口：`POST /retrieve`（requirement + library_id + build_id + eligible_tag_ids → candidates + rank_features + facets + family）、`POST /build`、`POST /activate`、`GET /stats`、`GET /health`
  - 验收：一次 HTTP 检索返回带特征候选；空白名单返回空；不混 build

- [ ] **T4-2 Spring Boot ↔ Python 调用**
  - 交付：`GET /taglibrary/semantic/index-build/{buildId}/stats` 经受控 Python 运维接口取实体 Collection/加载状态/ID 对账/alias；Python 不可用时**明确报暂不可用**，不兜底全量
  - 验收：权限、超时、错误语义明确

- [ ] **T4-3 请求固定 build + Trace**（方案 §7.4）
  - 交付：`trace_id / snapshot_id / build_id / store_type / 资格集合摘要 / 各通道排名 / margin / channel_agreement / family_resolved / unresolved_fuzzy_terms` 落 `ts_retrieval_feedback`
  - 验收：任一 Trace 可回放到 `(snapshot_id, index_build_id, doc_template_version)`

- [ ] **T4-4 构建/激活回写**
  - 交付：Python 构建完成 → Java `registerBuild`/`updateBuildStatus`（不能直接设 ACTIVE）；activate 由 Java 执行
  - 验收：状态机 `BUILDING→READY→ACTIVE` 不可跳步

- [ ] **T4-5 catalog_overview**（方案 §7.5）
  - 交付：按当前资格动态返回域/概念/标签计数，不泄露不可见成员数量

- [ ] **T4-6 分级召回参数化**
  - 交付：域软先验权重、Top-K 预算入 build 配置（权重计入 `retrieval_config_hash`）

### B5 反馈闭环（方案 §7.2-⑥、§8-S10）

- [ ] **T5-1 feedback 落库接口**
  - 现状：表在，**无 domain/mapper/service/controller**
  - 交付：`POST /taglibrary/semantic/feedback`：脱敏、身份取认证上下文、校验 trace/build/库与标签归属
  - 验收：伪造 user_id 无效；查询文本脱敏留存

- [x] **T5-2 ⑥ 历史纠错通道**
  - 交付：接受率 ≥0.9 且 n≥5 仅作草稿挖掘阈值 → 去重、脱敏、业务复核后物化别名/term；①已计入证据不重复加分
  - 验收：不自动进快照

- [ ] **T5-3 飞轮运维流程**
  - 交付：周期/最小样本/污染过滤（封存集隔离）→ DRAFT 候选 → S6 复核 → 下次发布

### B6 前端（方案 §11 末）

- [ ] **T6-1 标签语义维护页**
  - 交付：按概念/族分组展示草稿与复核；码值区间/层级编辑；别名/易混淆/示例/词典分页签
  - 复用：`ruoyi-ui/src/api/taglibrary/semantic.js`（当前无任何文件引用）

- [ ] **T6-2 快照与索引页**
  - 交付：快照列表、门禁报告、索引构建状态与评测摘要、激活/回滚操作

- [x] **T6-3 菜单与路由**
  - 现状：菜单 2140–2144 **全是 F 权限位**，无页面菜单
  - 交付：两个 C 型页面菜单 + 角色授权（新增迁移，幂等）

- [x] **T6-4 api 补全**
  - 交付：`semantic.js` 补 review / bootstrap export·import / index-build 注册·状态·stats / download 等方法

### B7 评测与验收（`[阻断]` 竞赛口径，方案 §10）

- [ ] **T7-1 测试集建设**
  - 现状：`eval/gold_retrieval_v1.json` 是 `status=PLACEHOLDER`、`queries=[]`；仅 20 条 `GOLD_DEV`
  - 交付：封存集 600 条（原子条件、可接受 tag 集、family_key、必须澄清/不可表达、期望 code 集合、资格上下文）；`hard_negative_auto`；`scale_gradient`（300/969/2828）；`alias_ablation`
  - 验收：封存集与开发集/LLM/反馈物理隔离

- [ ] **T7-2 eval runner 扩展**
  - 交付：Recall@20（记为 Hit@20）与"多字段条件完整命中"分列报告；族级@10；澄清/不可表达的正确处置率与误自动执行率；契约回归矩阵（空白名单 / 部分可见 / 标签下线 / 来源 CHANGED / 同码改义 / 跨桶 / 前导零 / NULL / 冷启·切换失败·降级）
  - 验收：越权、未上线、失效来源引用、分档错误自动转换为 **0**

- [ ] **T7-3 三类质量报告**
  - 交付：`semantic_quality_report`、`retrieval_eval_report`、`contract_regression_report`（含样本分母、失败数、置信区间、原始 ID 集与配置 hash）

- [ ] **T7-4 全量重建与回滚演练**
  - 交付：丢弃派生索引全量重建 → 记录耗时/RTO；回滚走同一激活流程
  - 验收：回滚后仍受当前权限/来源校验约束

- [ ] **T7-5 指标门槛对照**（方案 §2.1/§9.2）
  - 目标：候选 Recall@20 ≥ 99.5%（Gold）；族级 Recall@10 ≥ 99%；幻觉/越权/未发布引用率 = 0；黄金场景完整度 ≥90、发布范围均分 ≥70（比赛前 ≥85）；969 覆盖

### B8 运维与部署（方案 §6.2/§7.7.2）

- [ ] **T8-1 激活协议加固**
  - 现状：`activate()` 仅 DB 状态翻转（退旧 ACTIVE、置新 ACTIVE）
  - 交付：按库串行化（DB 条件更新/分布式锁）、Milvus alias 切换、失败补偿与对账、等待在途请求释放、回滚同流程
  - 验收：崩溃后按 DB 记录对账恢复；Miivus 与 DB 不一致可自查

- [ ] **T8-2 build/Collection 清理**
  - 交付：保留最近 3 个可回滚 build + 使用中的；异步受控清理（先对账），保留快照/manifest 供重建

- [ ] **T8-3 部署与资源**
  - 交付：`dev.sh` 可选接入 Milvus 检查；生产评估（4 核/16GB、磁盘、构建峰值、与 MySQL/Redis 同机可行性实测）

- [ ] **T8-4 监控告警**
  - 交付：standalone/etcd/minio + 构建/激活事件指标与告警

- [ ] **T8-5 长白名单性能**
  - 交付：必要时分批检索 + 全局 Top-K 合并；**不得省略过滤或把角色写入索引**

- [ ] **T8-6 备份与可重建**
  - 交付：`ry.ts_*` + 快照文件 + docs/词典/模型/配置；演练全量重建与恢复时间核定

### B9 治理与文档同步

- [x] **T9-1 文档同步**：方案附录 C 代码证据更新；本清单的偏差项（如 JSONL 存储选型）记入方案附录
- [ ] **T9-2 权限矩阵复核**：`semantic:list/edit/review/publish/bootstrap` 与角色绑定一致
- [ ] **T9-3 前后端权限对齐**：页面按钮 perms 与 `@PreAuthorize` 一一对应

---

## 3. 最小可运行路径（MVP，约 1 周）

目标：把"31 标签试点"从 CLI 升级为**线上快照 → 索引 → HTTP 检索**的闭环。

1. T0-1 ~ T0-4（迁移 + 核验 + 冒烟）
2. T1-1、T1-2、T1-3、T1-4（快照落盘 + 导出补全 + download + Python 取用）
3. T4-1（最小 `/retrieve`）+ T4-3（Trace）
4. 用试点快照跑一次 HTTP 检索 + 空白名单/部分可见契约回归

达成标志：`publish → download → build → retrieve` 四步有可复现命令与日志，`store_type=LOCAL` 明确标注。

## 4. 完整验收路径（对齐方案 §12 六周）

W1：B0 + T1-1~T1-4 + T3-3 PoC + T2-1/T2-2 小批
W2：T1-5/T1-6 + T2-3 试点概念 + T3-4 + LOCAL/MILVUS 双基线
W3：T2-5/T2-6/T2-7 + T6-1 + T2-8 门禁
W4：T3-5/T3-6 + T4-1~T4-6 + 开发集 300 条
W5：T5-1~T5-3 + T6-2/T6-3 + T7-2 契约回归 + T8-1 演练
W6：T2-* 全量复核收口 + T7-1/T7-3/T7-5 封存验收 + T8-3~T8-6 生产评估

## 5. 风险与对策（本次核对新增）

| 风险 | 对策 |
|---|---|
| 快照生成方双轨（Java 骨架 / Python 完整）长期并存 | T1-2 收敛到 Java 单一导出方；Python 组装仅保留离线试点用途 |
| `store_type` 未标注导致误以为 Milvus 在跑 | T3-1 在构建、快照、请求三层强制标注 |
| 依赖为空导致"能跑"的假象（仅 pytest 环境） | T3-4 锁版本后重跑全链路 |
| 运行态核验长期缺失，方案假设与库内事实漂移 | T0-2 产出书面核验记录并作为发布门禁输入 |
| 画像/反馈泄露客户信息 | 沿用方案 §13：原值禁出、低频抑制、最小权限、限期留存 |


## 6. 2026-09-19 实施回填

本次已交付核心 Java/Python/Vue 工程与全量规则草稿，执行本地增量迁移、在线来源冻结、受控草稿导入、外部 Milvus 隔离 PoC 和回归。详细逐批状态、命令、实测数据与未完成项见 [建设验收记录](../validation/语义索引层建设验收记录.md)；运行与恢复见 [ai-runtime/README.md](../../ai-runtime/README.md)。

- 当前真实库：970 条标签语义、1723 条码值语义、676 条概念候选，均 DRAFT；词典 40 REVIEWED；别名 0。旧“31/543/17”是试点基线，不能继续作为当前数量。
- 发布覆盖仍为 **0/969**；584 条口径未决项尚需业务确认。
- 续验 Java 186 / Python 66 项回归通过，前端生产构建通过；真实 BGE-M3/reranker 已完成开发 A/B、隔离 TCP 和外部 Milvus 实测。Milvus PoC 仅 3 文档、开发检索仅 20 条，不能代替全库/封存 600 验收。业务发布仍为 0/969，最终目标 969/969。
- 新迁移 `V20260919_01/02` 已本地应用；不修改宽 `tl_tag`，不恢复无主表，未运行 Docker 命令。
- 尚未关闭的复选框表示该任务的**完整验收**仍未完成，不表示代码均未实现。模型实测、业务复核、封存集、生产容量/故障演练不能以单元测试代替。
